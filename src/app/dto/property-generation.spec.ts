import { PropertyGeneration } from './property-generation';

describe('PropertyGeneration', () => {
  it('should create an instance', () => {
    expect(new PropertyGeneration()).toBeTruthy();
  });
});
